import { TooptipDirective } from './tooptip.directive';

describe('TooptipDirective', () => {
  it('should create an instance', () => {
    const directive = new TooptipDirective();
    expect(directive).toBeTruthy();
  });
});
